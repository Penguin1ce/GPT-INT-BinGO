<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firefly.ragdemo.mapper.UploadedFileMapper">

    <resultMap id="UploadedFileResult" type="com.firefly.ragdemo.entity.UploadedFile">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="filename" column="filename" />
        <result property="filePath" column="file_path" />
        <result property="fileSize" column="file_size" />
        <result property="fileType" column="file_type" />
        <result property="uploadTime" column="upload_time" />
        <result property="kbId" column="kb_id" />
        <result property="status" column="status" />
    </resultMap>

    <select id="findById" resultMap="UploadedFileResult">
        SELECT * FROM uploaded_files WHERE id = #{id}
    </select>

    <select id="findByUserIdOrderByUploadTimeDesc" resultMap="UploadedFileResult">
        SELECT * FROM uploaded_files
        WHERE user_id = #{userId}
        ORDER BY upload_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1) FROM uploaded_files WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.firefly.ragdemo.entity.UploadedFile">
        INSERT INTO uploaded_files (
            id, user_id, filename, file_path, file_size, file_type, upload_time, status, kb_id
        ) VALUES (
            #{id}, #{userId}, #{filename}, #{filePath}, #{fileSize}, #{fileType}, #{uploadTime}, #{status}, #{kbId}
        )
    </insert>

    <update id="updateStatus">
        UPDATE uploaded_files SET status = #{status} WHERE id = #{id}
    </update>

    <update id="update" parameterType="com.firefly.ragdemo.entity.UploadedFile">
        UPDATE uploaded_files SET
            user_id = #{userId},
            filename = #{filename},
            file_path = #{filePath},
            file_size = #{fileSize},
            file_type = #{fileType},
            upload_time = #{uploadTime},
            kb_id = #{kbId},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM uploaded_files WHERE id = #{id}
    </delete>
</mapper> 
